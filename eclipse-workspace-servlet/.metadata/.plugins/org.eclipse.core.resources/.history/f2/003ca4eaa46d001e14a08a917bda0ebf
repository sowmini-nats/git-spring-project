package com.demo.MavenDemo;

import static org.junit.Assert.assertEquals;

public class CalculateTest {
	
@Test
public void testAddNums() {
	Calculate obj = new Calculate();
	assertEquals(153,obj.addNum(7,8));
}
}